# SQLite local (file: é o prefixo do libSQL). Em Vercel/CF o fallback é /tmp (efêmero).
SQLITE_DB_PATH=sqlite.db

# Produção persistente (Turso / libSQL remoto)
# DATABASE_URL=libsql://seu-db.turso.io
# DATABASE_AUTH_TOKEN=
